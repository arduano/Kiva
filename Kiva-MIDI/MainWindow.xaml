<Window x:Class="Kiva_MIDI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Kiva_MIDI"
        xmlns:i="clr-namespace:System.Windows.Interop;assembly=PresentationCore"
        xmlns:dxc="clr-namespace:SharpDX.WPF;assembly=SharpDX.WPF"
        xmlns:wpf="clr-namespace:SharpDX.WPF"
        mc:Ignorable="d"
        Name="mainWindow"
        Title="MainWindow" WindowStyle="None" Height="450" Width="800" Loaded="Window_Loaded" SnapsToDevicePixels="True" PreviewKeyDown="MainWindow_PreviewKeyDown">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary>
                    <BitmapImage x:Key="Icon" UriSource="i2m.png" />
                </ResourceDictionary>
                <ResourceDictionary Source="WindowButtonTemplate.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="{Binding ActualHeight,ElementName=titlebar}"/>
    </WindowChrome.WindowChrome>
    <DockPanel LastChildFill="True">
        <Border Visibility="{Binding ChromeVisibility, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}" Background="#0068c9" DockPanel.Dock="Top" Height="50" x:Name="titlebar">
            <DockPanel>
                <Label Name="fpsLabel"></Label>
                <!--<Image HorizontalAlignment="Center" Width="40" Height="Auto" Margin="5" Source="i2m.png"/>-->
                <DockPanel DockPanel.Dock="Right" HorizontalAlignment="Right" Margin="0,0,10,0">
                    <Button Style="{StaticResource WindowButton}" Focusable="False" Name="ExitButton" Background="Red" Width="20" Height="20" Margin="3" DockPanel.Dock="Right" WindowChrome.IsHitTestVisibleInChrome="True" Click="ExitButton_Click"></Button>
                    <Button Style="{StaticResource WindowButton}" Focusable="False" Name="MinimiseButton" Background="Orange" Width="20" Height="20" Margin="3" DockPanel.Dock="Right" WindowChrome.IsHitTestVisibleInChrome="True" Click="MinimiseButton_Click"></Button>
                </DockPanel>
                <Grid DockPanel.Dock="Left">
                    <Grid VerticalAlignment="Top" HorizontalAlignment="Left" WindowChrome.IsHitTestVisibleInChrome="True">
                        <!--<Button Style="{StaticResource MenuButtonStyle}" Click="MenuButton_Click" Visibility="Collapsed">
                            <Grid>
                                <Label>
                                    File
                                </Label>
                                <Popup StaysOpen="False">
                                    <Button>Test</Button>
                                </Popup>
                            </Grid>
                        </Button>-->
                    </Grid>
                </Grid>
            </DockPanel>
        </Border>
        <Grid Name="glContainer">
            <!--<local:DXImage x:Name="dx11img">
            </local:DXImage>-->
            <local:DXElement x:Name="dx11img">
            </local:DXElement>
        </Grid>
    </DockPanel>
</Window>
